import { Image, Header } from 'semantic-ui-react';

const Banner = props =>{
	return (
		<header className={'center aligned'}>
			<Image src={''}/>
			<Header as={'h1'}>Joe Nyugoh</Header>
		</header>
	);
};

export default Banner;
